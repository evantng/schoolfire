/*
This scene is a variation on the Feature Demo that demonstrates how get text input
from the player during a conversation.

You may want to familiarize yourself with the original Feature Demo scene first.

This version a database named: 'Feature Demo with Text Input'. It contains:

o A new variable "MyName".

o Modifications to two entries in Private Hart's conversation:

  1. Entry [8] contains a TextInput() sequencer command to request text input,
     which gets stored in the MyName variable.
  2. Entry [11] uses the value MyName in its dialogue text.

o In addition, see the Text Field UI objects in the dialogue UI. This shows how
  to set up your input controls.

*/